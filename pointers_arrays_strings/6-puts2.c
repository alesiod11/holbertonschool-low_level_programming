#include "main.h"
/**
 * puts2 -  prints every other character of a string
 * @str: input string
 */
 void puts2(char *str);
 
{
    int 1;

    i = 0
    while (str[i] != '\0')
    {
        if (i % 2 == 0)
        _putchar(str[i]);
    }
}